Это две команды для регистрации ваших данных в Git, что бы при пуше вашиз изменений он не
запрашивал у эти данные постоянно, пишутся один раз после установки самого Git:

git config --global user.name "Your name"

git config --global user.email "your-email@example.com"


Основные команды для работа с Git

1) Это команды для инициализации Git в вашем новом проекте, пишется в один раз в начале после создания проекта:
git init

2) Это команда для привязки вашего проекта с вашим репозиторием, пишется один раз после команды git init.
в конце команды нужно будет вставить вашу ссылку на ваш репозиторий:
git remote add origin Link

3) Это команды для проверки состояния ваших файлов в проекте.
git status

4) Это команда для добавления в буфер ваших новых файлов и изменений в файлах. после команды add через
пробел ставите точку для того чтобы добавить все файлы:
git add .

5) Это команда для сохранения ваших данных которые находятся в буфере. Внутри кавычек пишите название вашего комита:
git commit -m "Commit name"

6) Это команда для просмотра на какой ветке вы находитесь и какие ветки есть в вашем репозитории:
git branch

7) Это команда для заливки вашего проекта на ваш репозиторий. После команды origin через пробел
пишите название вашей ветки, оно отображается в вашем терминале в скобке (master), (main) или (что-то другое):

git push origin master

8) это команда для удаления ваших файлов из вашего проекта. после нужно снова сделать команду git add . и запушить:
git rm --cached -r .idea


9) git rebase -i HEAD~2 для удаления коммита
drop 5fa5048fc55139edf861b47d20edbb309a7c186c Lesson 2.1
pick afe65062afa959b0efbd409c17a3a620691d3faa Test 2

git push --force origin master

git rebase --edit-todo
git rebase --continue

rm -rf имя_папки


10)

